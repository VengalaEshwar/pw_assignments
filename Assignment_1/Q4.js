/*
*comma operator allow us to seperate various declartions
*of varibles in a single statement without declaring again.
*basically comma operator is uses to seperate the variables.
*/
for(let i=0,j=0;i<10;i++,j++)
{
    console.log(`i=${i} j=${j}`);
}
